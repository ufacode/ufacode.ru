<div class="container">
  <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
    <h1>Пользователи</h1>
    <table class="table">

      <% @users.each do |user| %>
        <tr>
          <td><%= link_to user.name, user %></td>
        </tr>
      <% end %>

    </table>
    <hr>
    <%= paginate @users %>

  </div>
</div>
